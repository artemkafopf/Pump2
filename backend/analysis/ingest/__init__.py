"""Входной слой: сборка Свода и источников из сырых выгрузок.

Перенесено из ``d:\\GitHub\\db_builder`` (пакет ``failure_update`` и его
зависимости), чтобы обработка входа и расчёт модели жили в одном репозитории и
не расходились. Warehouse-слой db_builder сюда не переносился — он здесь не
нужен.

Состав:

``svod``        сборка регистра ЭЦН из ПДК + паспорта оборудования (Big) +
                телеметрии + ТехРежима + лаборатории + ОПЗ + ГРП;
``telemetry``   слияние суточных выгрузок телеметрии и их SQLite-хранилище;
``techregime``  то же для ТехРежима;
``lab``         то же для лабораторной химии;
``frac``        реестр ГРП (``Свод ГРП``) — полная выгрузка плюс признак
                «был ли ГРП на скважине».

Все пути берутся из :mod:`analysis.paths`; хардкодов ``d:\\Projects\\...`` здесь
нет. Тяжёлая логика живёт тут, тонкие CLI — в ``scripts/ingest/``.
"""

__all__ = ["frac", "lab", "svod", "techregime", "telemetry"]
