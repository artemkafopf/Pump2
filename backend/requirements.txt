fastapi==0.116.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.43
psycopg[binary]==3.2.9
pandas==2.3.2
numpy==2.3.2
catboost==1.2.8
python-multipart==0.0.20
pydantic-settings==2.10.1
openpyxl==3.1.5
xlrd==2.0.1
# Read-only Excel engine used by analysis.ingest; several times faster than
# openpyxl on the 90 MB telemetry drop. analysis.ingest.excel_io falls back to
# the default engine when it is absent, so this is a speed dependency, not a
# hard one.
python-calamine==0.5.3
httpx==0.28.1
scipy==1.16.1
matplotlib==3.10.5
seaborn==0.13.2
plotly==6.3.0
lifelines==0.30.3
scikit-learn==1.9.0
statsmodels==0.14.6
